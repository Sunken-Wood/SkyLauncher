<UserControl x:Class="SkyLauncher.Views.MinecraftSettingPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SkyLauncher.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding MinecraftName}" Grid.Row="0" Margin="10"></TextBlock>
        <ListView Grid.Row="1" 
                  ItemsSource="{Binding Mods}" 
                  Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="500" Header="名称" DisplayMemberBinding="{Binding FileName}"/>
                    <GridViewColumn Width="130" Header="大小" DisplayMemberBinding="{Binding FormattedSize}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
