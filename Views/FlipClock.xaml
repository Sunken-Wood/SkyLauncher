<UserControl x:Class="SkyLauncher.Views.FlipClock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SkyLauncher.Views"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border CornerRadius="4" Padding="10" Margin="10" Background="{DynamicResource RegionBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions >
            <Viewbox MaxWidth="520" Grid.Row="0">
                <hc:FlipClock Margin="32"/>
            </Viewbox>
            <StackPanel Grid.Row="1">
                <Button Margin="10" Height="40" HorizontalAlignment="Center" Width="150" Style="{StaticResource ButtonDefault}" Click="GoBack">
                    <DockPanel>
                        <iconPacks:PackIconLucide Kind="ArrowLeft" Foreground="{DynamicResource PrimaryNormalBrush}" Width="18" Height="18"/>
                        <TextBlock Text="返回" Margin="12,0,0,0" VerticalAlignment="Center"/>
                    </DockPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
